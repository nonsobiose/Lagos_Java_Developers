<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!-- Developers_Name_theme. -->
    <style name="Developers_Name">
        <item name="android:fontFamily">sans-serif</item>
        <item name="android:gravity">center</item>
        <item name="android:textColor">@color/black</item>
        <item name="android:textSize">30sp</item>
        <item name="android:text">DUMMY TEXT</item>
    </style>

    <!-- Developer_Link_theme. -->
    <style name="developer_weblink">
        <item name="android:background">@color/colorAccent</item>
        <item name="android:gravity">center</item>
        <item name="android:text">Dummy Text</item>
        <item name="android:textAllCaps">true</item>
        <item name="android:textSize">15sp</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:textStyle">bold</item>
    </style>

    <!-- Location_title theme. -->
    <style name="Location_title">
        <item name="android:text">LOCATION</item>
        <item name="android:textColor">@color/black</item>
        <item name="android:textSize">15sp</item>
        <item name="android:textAllCaps">true</item>
    </style>

    <!-- Location_text_theme. -->
    <style name="Location_text">
        <item name="android:layout_marginBottom">5dp</item>
        <item name="android:text">Lagos, Nigeria</item>
        <item name="android:textColor">@color/location_repos_color</item>
        <item name="android:textSize">18sp</item>
        <item name="android:textStyle">bold</item>
    </style>

    <!-- Repos_title theme. -->
    <style name="Repos_title">
        <item name="android:text">REPOS</item>
        <item name="android:textColor">@color/black</item>
        <item name="android:textSize">15sp</item>
        <item name="android:textAllCaps">true</item>
    </style>

    <!-- Repos_text_theme. -->
    <style name="Repos_text">
        <item name="android:layout_marginBottom">5dp</item>
        <item name="android:text">89</item>
        <item name="android:textColor">@color/location_repos_color</item>
        <item name="android:textSize">18sp</item>
        <item name="android:textStyle">bold</item>
    </style>

</resources>
